# ==========================================
# AgentPaul - Variáveis de Ambiente
# ==========================================

# OpenAI (OBRIGATÓRIO)
OPENAI_API_KEY=sk-...

# Modelo do agente (opcional, padrão: gpt-4o)
AGENT_MODEL=gpt-4o

# URL do backend NutriBuddy (OBRIGATÓRIO)
BACKEND_URL=https://web-production-c9eaf.up.railway.app

# Secret para autenticação com o backend (OBRIGATÓRIO)
WEBHOOK_SECRET=nutribuddy-secret-2024

# Porta (Railway define automaticamente)
PORT=3001

# Debug mode (mostra logs detalhados)
DEBUG=true

# Ambiente
NODE_ENV=production
